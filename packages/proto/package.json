{
  "name": "proto",
  "type": "module",
  "exports": {
    "./peers/v1": {
      "types": "./dist/peers/v1/index.d.ts",
      "import": "./dist/peers/v1/index.js"
    },
    "./events/v1": {
      "types": "./dist/events/v1/index.d.ts",
      "import": "./dist/events/v1/index.js"
    }
  },
  "scripts": {
    "build": "tsdown --out-dir ./dist",
    "build:generate": "npm run proto:generate && tsdown --out-dir ./dist",
    "proto:generate": "buf generate",
    "dev": "npm run build:generate -- --watch src",
    "lint": "oxlint --fix",
    "format": "buf format --write"
  },
  "devDependencies": {
    "@rthings/config": "workspace:*",
    "@types/bun": "catalog:",
    "oxfmt": "catalog:",
    "oxlint": "catalog:",
    "tsdown": "catalog:"
  },
  "peerDependencies": {
    "typescript": "catalog:"
  },
  "dependencies": {
    "@bufbuild/buf": "catalog:",
    "@bufbuild/protobuf": "catalog:",
    "@bufbuild/protoc-gen-es": "catalog:"
  }
}
