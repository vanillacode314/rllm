import { defineConfig } from 'oxfmt';

export default defineConfig({
  printWidth: 100,
  singleQuote: true,
  sortImports: true,
  sortPackageJson: false,
  trailingComma: 'none',
  useTabs: false
});
