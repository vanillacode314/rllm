# What's New

## v0.0.18 — August 7, 2026

### 🎨 Improvements

- **Attach files from your phone** — the attach-file button now appears on small screens, so you can add files to your chats on mobile, too.

### 🐛 Bug Fixes

- **Android: document search stays reliable** — fixed an issue where saved search data could get corrupted when stored on Android, which could break document search. (This completes the fix from the last update so search data always survives storage intact.)
