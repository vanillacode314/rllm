/** Interval in ms to recheck failed proxy (default 5 min) */
export const PROXY_HEALTH_CHECK_INTERVAL_MS = 5 * 60 * 1000;

/** Timeout for proxy health check request (default 10s) */
export const PROXY_HEALTH_CHECK_TIMEOUT_MS = 10 * 1000;
