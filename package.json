{
  "name": "rllm",
  "private": true,
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "format": "turbo format",
    "lint": "turbo lint",
    "test": "turbo test",
    "postinstall": "bunx bunch-package apply"
  },
  "workspaces": [
    "packages/*",
    "apps/*",
    "apps/web/*"
  ],
  "packageManager": "bun@1.2.5",
  "devDependencies": {
    "bunch-package": "^1.1.2",
    "turbo": "^2.10.9"
  }
}
